<script src="api-all.js"></script>
<script>
    async function loadData() {
        console.log("ရှာဖွေနေပါသည်...");
        const items = await searchProducts('shoes');
        
        if (items.length > 0) {
            console.log('အောင်မြင်သည်! ပစ္စည်းအရေအတွက်:', items.length);
            // ဒီမှာ items တွေကို Grid ထဲ ထည့်ပြပါ
        } else {
            console.log('မတွေ့ပါ သို့မဟုတ် Error တက်နေပါသည်');
        }
    }
    
    loadData();
</script>
